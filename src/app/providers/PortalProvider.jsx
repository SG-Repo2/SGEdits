/**
 * PortalProvider.jsx
 * Demo mode removed — AceDAT Portal is live-only (Supabase).
 * This file now re-exports from SupabaseProvider so that all existing
 * page imports (usePortal, PortalProvider) continue to work without changes.
 */
export { SupabaseProvider as PortalProvider, usePortal } from './SupabaseProvider';
